<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 인터페이스 BoardMapper를 구현하는 자식 xml -->
<mapper namespace="edu.global.ex.mapper.BoardMapper"> <!-- 패키지명.인터페이스명 -->
	<select id="getList" resultType="edu.global.ex.vo.BoardVO"> <!-- resultType는 interface에서의 함수 데이터타입과 맞춰준다. -->
  		<![CDATA[
  			select * from mvc_board order by bgroup desc, bstep asc
  		]]>
	</select>
	<!-- My batis를 사용하면 Connection, Statement, ResultSet가 캡슐화되어있어서 선언안해도 기능이 
		적용되어있다. -->

	<select id="read" resultType="edu.global.ex.vo.BoardVO"> <!-- 패키지명.클래스명 -->
  		<![CDATA[
  			select * from mvc_board where bid = #{bid} 
  		]]>
		<!-- 파라미터(데이터) 넘길 때는 #으로 -->
	</select>

	<update id="update"> <!-- select문일 때는 <select>, update문일 때는 <update> -->
  		<![CDATA[
  			update mvc_board set bname=#{bname}, btitle=#{btitle}, bcontent=#{bcontent} 
  			where bid=#{bid}
  		]]>
	</update>

	<delete id="delete">
  		<![CDATA[
  			delete from mvc_board where bid=#{bid}
  		]]>
	</delete>

	<insert id="insert">
  		<![CDATA[
  			insert into mvc_board (bid, bname, btitle, bcontent, bhit, bgroup, bstep, bindent) 
            values (mvc_board_seq.nextval, #{bname}, #{btitle}, #{bcontent}, 0, mvc_board_seq.currval, 0, 0) 
  		]]>
	</insert>

	<!--댓글을 하나씩 미뤄서 정렬을 해줘야 하기 때문에 세로 정렬을 bstep에 +1로 한칸씩 미뤄내줘야한다. -->
	<update id="updateShape">
      <![CDATA[
          update mvc_board set bstep = bstep + 1 where bgroup =#{bgroup} and bstep > #{bstep}
      ]]>
	</update>

	<insert id="insertReply">
   <![CDATA[
      insert into mvc_board (bid, bname, btitle, bcontent, bgroup, bstep, bindent) 
      values (mvc_board_seq.nextval, #{bname}, #{btitle},#{bcontent}, 
                #{bgroup}, #{bstep}+1, #{bindent}+1)
   ]]>
	</insert>

	<select id="getTotalCount" resultType="int"> 
  		<![CDATA[
  			select count(*) from mvc_board 
  		]]>
	</select>

	<select id="getListWithPaging" resultType="edu.global.ex.vo.BoardVO">
      <![CDATA[
        SELECT * FROM (
              SELECT ROWNUM AS RNUM, A.* FROM (
                       SELECT
                             *
                         FROM
                             mvc_board 
                         order by bGroup asc, bStep desc   
             ) A WHERE ROWNUM <= #{pageNum} * #{amount}
         ) WHERE RNUM > (#{pageNum}-1) * #{amount}
      ]]>
      <!-- 테이블 전부의 데이터가 아니라 해당 페이지에 1~10개씩 데이터를 뿌려주는 코드 -->
	</select>

</mapper>